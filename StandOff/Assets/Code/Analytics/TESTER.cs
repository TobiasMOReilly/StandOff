using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TESTER : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        AnalyticsManager.TestAnalyitc();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
